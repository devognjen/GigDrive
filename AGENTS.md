Be aware of the provided PRD.md file.

- Follow the best practices and use the idiomatic approach to develop the Nest.js and Angular application
- When writing code, try to follow the SOLID principles and to write Clean Code (as introduced by Robert C. Martin)
- Try to cover all the application features with unit tests
- Feature specifications live in `docs/features/` (one .md file per feature, extracted from the PRD)
- Implementation progress is tracked in `PROGRESS.md` (single source of truth for feature status) — keep it updated as features are implemented
- Use pnpm as the package manager
