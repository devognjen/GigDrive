# Development image: runs ng serve, sources mounted by compose.
FROM node:24-alpine
WORKDIR /app
RUN corepack enable
COPY package.json pnpm-lock.yaml ./
RUN pnpm install --frozen-lockfile
COPY . .
EXPOSE 4200
CMD ["pnpm", "start", "--", "--host", "0.0.0.0", "--poll", "2000"]
